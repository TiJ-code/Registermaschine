## CORE
- added `IPlugin#onDisable` call to already loaded plugins

- added `IPlugin#onDisable` call to already loaded plugins
- refactored Plugin loading lifecycle, to handle errors that might prevent other plugins from getting properly disabled

## API
- added API documentation to `IPlugin`
- added API documentation to `PluginContext`

