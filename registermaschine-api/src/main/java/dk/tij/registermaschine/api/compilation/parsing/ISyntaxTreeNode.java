package dk.tij.registermaschine.api.compilation.parsing;

/**
 * Represents a node within a syntax tree.
 *
 * <p>A node models a structural element of the parsed input, such as
 * an instruction, operand, label, or other language construct.</p>
 *
 * <p>The exact properties and relationships of nodes are defined by
 * the parser implementation. This interface serves as a common marker
 * for all syntax tree elements.</p>
 *
 * @since 1.0.0
 * @author TiJ
 */
public interface ISyntaxTreeNode {
}
